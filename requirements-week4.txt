# Week 4 — internal ADR § 1 + § 4 順守 (FastAPI UI + Vault Fernet active 化)
# doctrine: python-supply-chain-5layer 5 layer 順守 + T4 inherit pattern (FastAPI + Jinja2 + cryptography pin)

# === Week 3 inherit ===
-r requirements-week3.txt

# === Week 4 新規 active (T1-T4 inherit、 Standard Pin) ===
# FastAPI Web UI (T1-T4 cross-PJ inherit pattern、 doctrine: prior-art-first)
fastapi>=0.136.1,<1.0
# uvicorn ASGI server (standard extras = httptools + uvloop + websockets)
uvicorn[standard]>=0.30,<1.0
# python-multipart (FastAPI Form() で literal 必須、 file upload + form data)
python-multipart>=0.0.18,<1.0
# Fernet 暗号化 (src/vault/ + assistant_vault.jsonl、 T1-T4 inherit、 CVE-fix 済 pin)
cryptography>=46.0.6,<47.0
